import { userTypes } from '../models/user/types';
import { tiposAvance } from '../models/avances/types'
import { projectTypes } from '../models/project/types';
import { enumTypes } from '../models/enums/types';
import { inscripcionTypes } from '../models/inscripcion/types';
export const types = [userTypes, projectTypes, enumTypes, inscripcionTypes, tiposAvance];
